{
  "name": "@glue/core",
  "version": "0.0.1",
  "description": "G.L.U.E. federation substrate — registry, discovery, capability graph, router, policy, trust, provenance, lifecycle",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@glue/contracts": "*",
    "zod": "^3.22.4",
    "uuid": "^9.0.1",
    "pino": "^8.19.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  }
}
